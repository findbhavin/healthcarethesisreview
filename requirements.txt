flask==3.0.3
anthropic==0.40.0
google-genai==1.13.0
python-docx==1.1.2
pdfplumber==0.11.4
pyyaml==6.0.1
gunicorn==23.0.0
reportlab==4.2.5
google-cloud-storage==2.18.2
# Razorpay is called directly via REST (urllib) — no SDK needed. This avoids
# the razorpay 1.x dependency on pkg_resources, which was removed from
# setuptools 81 and broke `/payment/create-order` on python:3.12-slim.
